# Optional: hard-code your OpenRouter API key at build time.
# Users can also enter the key at runtime via the ⚙️ button in the UI
# (stored in browser localStorage — no build needed).
#
# Get a key at https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here
